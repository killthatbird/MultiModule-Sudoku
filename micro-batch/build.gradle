plugins {
    id 'java'
    id 'application'
    id "org.flywaydb.flyway" version "4.2.0"
}
flyway {
    driver = 'com.mysql.cj.jdbc.Driver'
    user = 'root'
    url = 'jdbc:mysql://localhost:3306/quartz?useSSL=false'
}
mainClassName = 'jp.co.valtech.sudoku.batch.BatchApp'